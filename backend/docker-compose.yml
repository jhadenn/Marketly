services:
  # db:
  #   image: postgres:16
  #   container_name: marketly_db
  #   environment:
  #     POSTGRES_USER: marketly
  #     POSTGRES_PASSWORD: marketly
  #     POSTGRES_DB: marketly
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - marketly_pgdata:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U marketly -d marketly"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app


volumes:
  marketly_pgdata:
